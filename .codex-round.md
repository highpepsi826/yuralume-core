# Codex Round Checkpoint

This is a short-lived execution checkpoint for the current local repair. The
behavior specification remains in
`COMMITMENT_RECONCILIATION_IMPLEMENTATION_REFERENCE.md`.

## Cross-Channel Story Arc Planner (2026-08-31)

- Reference: `CROSS_CHANNEL_ARC_PLANNER_REFERENCE.md`.
- Goal: make normal LLM-planned story summaries use the unified web +
  Telegram + LINE timeline; abandoned-arc handling is explicitly unchanged.
- Completed source step: `StoryArcService._summarize_recent_dialogue()` now
  calls `recent_messages_for_character(..., exclude_tool_only=True)` and keeps
  the existing 40-message, tolerance-sanitized, fail-soft summary contract.
  Existing callers (new arc, regeneration, season context, autonomous
  recheck) therefore share the same cross-channel evidence.
- Completed test step: planner summary regression now seeds deterministic web,
  Telegram, and LINE messages and confirms chronological merge plus tool-only
  exclusion. Focused StoryArcService/planner/context tests passed 63; the
  cross-source repository tests passed 26; the broader story-arc/event/planner
  suite passed 276 through the local-permission test runner; a final focused
  `test_story_arc_dialogue_summary.py` + `test_story_arc_service.py` rerun
  passed 31. Compile and diff checks passed. Its sandbox-only migration fixture
  failures were Windows temp directory permissions, not source failures.
- Data safety: no conversation, arc, beat, schedule, memory, goal, promise,
  or cooldown data was changed. Deployment ran its standard migration gate but
  made no schema or live-data change for this read-path-only update.
- Current phase: source-only implementation committed as `300218c local: use
  cross-channel story arc context`; progress record committed as `65eebe7`.
  Deployment and runtime verification are now complete using build `4b78590`.
- Deployment verification: backup
  `pre-cross-channel-arc-planner-20260831-114811.dump` was verified; Compose
  rebuilt only app and recreated only migration/app; all services are healthy;
  `/health` returned `status=ok`; migration exited 0; Alembic is
  `u2c6m8p10046 (head)`; and the container source contains the unified
  `recent_messages_for_character` call.
- Next action: normal use. Do not modify live data, schedules, memories,
  promises, goals, or cooldown behavior as part of this completed repair.
- Blocked reason: none.

## Proactive Intention Judge Prompt Fix (2026-08-30)

- Reference: `PROACTIVE_INTENTION_JUDGE_PROMPT_FIX_REFERENCE.md`.
- Problem: `_optional_deferred_intents_block()` calls undefined `parts` when
  an active deferred intent has `revisit_at`, causing `intention judge raised`.
- Scope: one prompt-builder fix, regression tests, and app-only deployment;
  preserve deferred-intent data and the real-message 30-minute cooldown.
- Current phase: source fix, backup, deployment, and runtime verification
  complete; source commit `c574ddb` is deployed.
- Last test: intention judge 33 passed; dispatcher/deferred-intent/attempt/
  scheduler suite 77 passed; compile and `git diff --check` passed.
- Files changed: `src/kokoro_link/infrastructure/proactive/llm_intention_judge.py`,
  `tests/unit/test_llm_proactive_intention_judge.py`, and the scoped reference.
- Backup verified: `pre-proactive-intention-judge-fix-20260830-163419.dump`,
  SHA-256 `5BD4F2A95E4816BC77FA8B72323EABFC3952E39B643BF37825035A260BEF0DD2`.
- Deployment verified: image digest
  `sha256:251305ee23e3b2acd2c2c9317afd6801e6212865d3437f09c33f395a298377dc`,
  migrate exit 0, Alembic `u2c6m8p10046 (head)`, all services healthy,
  `/health`=`ok`, runtime commit `c574ddb`.
- Deferred intent remains active with its 17:30 HK revisit time; no new
  post-deployment `errored` attempt or `parts` NameError appeared.
- Next action: monitor normal proactive ticks; no further repair is pending.
- Blocked reason: none.

## First-Meeting Premature Completion Repair (2026-08-30)

- Reference: `FIRST_MEETING_PREMATURE_COMPLETION_FIX_REFERENCE.md`.
- Goal: keep the 17:30 first-meeting beat pending until the player is present,
  and undo only the false 00:02 autonomous event/memories that caused the arc
  to disappear from the active-story view.
- Confirmed data: beat `3364666069e5424fbf0e5d9a5faffb37` is
  `is_first_meeting=true`, `operator_position=NULL`, and was realized by
  `scene_simulation` at 00:02 HK; arc `6f727ca8102c493faf8af57344b6a7ae`
  became `completed` at 14:21 after all beats were terminal.
- Scope: source guard plus exact-ID transactional repair; no migration and no
  changes to unrelated chat, schedule, goal, promise, memory, or cooldown data.
- Current phase: source guard, exact live repair, deployment, and post-tick
  verification complete.
- Last test: focused first-meeting/story/proactive suite — 78 passed;
  background proactive/scheduler/tick/story suite — 148 passed; compile and
  `git diff --check` passed.
- Files changed for implementation: `src/kokoro_link/domain/entities/story_arc.py`,
  `src/kokoro_link/application/services/story_arc_service.py`,
  `src/kokoro_link/application/services/story_event_service.py`,
  `src/kokoro_link/application/services/story_beat_scene_service.py`,
  `src/kokoro_link/application/services/proactive_dispatcher.py`,
  `src/kokoro_link/bootstrap/container.py`, and focused tests.
- Backup verified: `pre-first-meeting-premature-completion-20260830-1517.dump`
  (custom format, SHA-256
  `995A3448EB573137BAD80EC74C1A4B2395EF2EEB7CB1429A4AE0921F19B35B22`).
- Only `app` was stopped during backup/repair; it is now recreated and healthy.
- Repair committed: `FIRST_MEETING_REPAIR_COMMITTED`; arc is active, target beat
  pending with zero attempts, false event/memories absent, valid stage events
  preserved, and exactly one live first-meeting schedule/beat.
- Post-repair counts: characters=1, daily_schedules=23,
  schedule_activities=293, story_arcs=4, story_arc_beats=31,
  story_events=30, memory_items=1444, character_goals=20,
  pending_follow_ups=53.
- Deployment verified: local image digest
  `sha256:535a0dc518a49b31a7b7bd56e61eb701abbbd5e16aa31e92e24510692a503706`,
  migrate exit 0, Alembic `u2c6m8p10046 (head)`, all services healthy,
  `/health`=`ok`, runtime commit `173b66c`.
- Natural post-restart tick completed successfully; target first-meeting beat
  remains pending with zero attempts before 17:30 HK. An unrelated intention
  judge crash was contained by the scheduler and remains a separate follow-up.
- Next action: monitor the 17:30 attended interaction; no further repair or
  migration is pending.
- Blocked reason: none.

## First-Meeting Reassessment Control (2026-08-30)

- Reference: `FIRST_MEETING_REASSESSMENT_REFERENCE.md`.
- Goal: repair the latch-only schedule-anchor conflict and add a confirmed
  operator-triggered reassessment path for pending story beats.
- Completed: `StoryEventService` now treats only `has_memory=true` as
  historical for a first-meeting anchor. A `memorialized=true,
  has_memory=false` row remains eligible when it is the unique exact
  commitment-key match; start-time and player-present guards are unchanged.
- Completed: the read-only manual recheck uses the existing rechecker protocol
  with a bounded, cross-channel (including Telegram) interaction summary;
  `delay_beat` and `skip_beat` never mutate from this path. A missing or empty
  evidence summary fails closed before the LLM call.
- Completed: preview and explicit confirmation routes; confirmation uses the
  established `StoryEventService` write path and returns an already-created
  event when a concurrent writer has completed the beat.
- Completed: per-pending-beat Story Arc panel control, result dialog, editable
  proposed event summary, and Traditional Chinese/English/Japanese labels.
- Last backend test: focused first-meeting/recheck/service/route suite —
  75 passed. Frontend reassessment API test, `vue-tsc -b`, and i18n checks
  passed; the local-permission Vite/PWA build passed. Python compile and
  `git diff --check` passed.
- Test stability: `test_pending_promise_reconciliation_updates_open_only` now
  injects its declared fixture clock; the six focused commitment
  reconciliation tests pass. This is a test-only change with no ChatService
  behavior change.
  - Source commit: `76a64cd local: preserve schedule commitment identity`.
  - Deployment complete: backup
    `pre-schedule-identity-hotfix-20260830-235206.dump` was verified as
    PostgreSQL custom format (SHA-256
    `56243118F06702584D434FDD8E08D7A6586B882A74AA88859C6B9A82FB6497BC`);
    built only the local app image, ran `migrate` successfully, and recreated
    only `app`. Support containers and volumes were retained.
  - Runtime verification: image
    `sha256:1a8bacde128cccd95e6846e4a3211a442d2bdb3ca14ad28d35b087e71933f934`;
    build SHA `76a64cd`; Alembic `u2c6m8p10046`; all Compose services healthy;
    `/health` returned `ok`; reassess/confirm routes appear in OpenAPI; the
    live mapper reconstructed the exact 17:30 activity with its commitment
    key and first-meeting flag; no traceback or error-level startup log appeared.
  - Post-deployment discovery: first live reassessment logged `matches=0`
    despite a valid exact DB activity. `sa_schedule_mapping._row_to_activity`
    drops `commitment_key` and `is_first_meeting`; its symmetric writer omits
    them too. This is an ORM round-trip bug, not a live data issue.
  - Completed source fix: restored both mapping directions with tolerant
    legacy reads; the 87-test focused mapping/commitment/first-meeting/recheck
    suite, source compile, and diff check passed.
  - Next action: commit, create a fresh verified backup, and deploy the
    app-only hotfix. Do not edit the valid live schedule row, beat, memories,
    promises, goals, or cooldown.
  - Blocked reason: none.

## Proactive Audit Recording Hotfix (2026-08-30)

- Goal: restore the missing `image_intent` imports that currently crash every
  proactive positive-send path before its audit row is written.
- Scope: one source import fix plus regression verification and approved app
  redeployment; no migration, row repair, or cooldown/policy change.
- Current phase: source fix and focused verification complete.
- Completed: restored the missing `image_intent` imports and the post-tool
  truthful fallback for attachment-less image commitments. The latter was
  also lost in the upstream merge and was caught by the existing regression.
  The image tool suite passed 15 tests; the proactive dispatcher/scheduler/
  tick suite passed 321 tests; compile and `git diff --check` passed.
- Deployment complete: committed `5dc8672`; backup
  `pre-proactive-audit-hotfix-20260830-140137.dump` verified; local image built;
  `migrate` exited 0; only `app` recreated. All Compose services are healthy,
  `/health` is `ok`, Alembic is `u2c6m8p10046 (head)`, and the first natural
  tick at 14:09:47 HK wrote a new `gate_blocked` audit row without the former
  `is_image_commitment` exception.
- Current phase: deployed and verified; monitor only.
- Blocked reason: none.

## Pending Follow-up Admin CRUD Checkpoint (2026-08-30)

- Goal: add a small admin-only CRUD escape hatch for queued
  `scheduled_promise` rows.
- Scope: source implementation plus approved local deployment; no feature
  migration or live user-data changes. Preserve the 30-minute proactive
  cooldown and all other schedule/story/goal/memory behaviour.
- Current phase: source implementation, verification, and deployment complete.
- Completed: domain `with_admin_edit`; strict in-memory/SQL admin insert,
  compare-and-set edit, slot census, and queued delete; admin DTOs/routes;
  container wiring; admin API client and panel controls; localized UI strings;
  turn-journal pending-row identity round-trip; compatibility fixes for the
  pre-existing chat image no-tool branch and legacy promise payloads.
- Safety rules: only `kind=scheduled_promise` + `status=queued` is mutable;
  future timezone-aware times and non-empty intent are required; existing
  conversation is required for add; no automatic cross-surface reconciliation;
  no migration; player card remains read-only.
- Last verified tests: admin service/routes + SQL adapter — 11 passed;
  admin-auth integration suite — 17 passed;
  pending/release/dispatcher/post-turn/snapshot focused run — 179 passed;
  ChatService — 31 passed. One known separate failure remains in
  `test_out_of_order_audits_never_merge_a_claim_undo_would_destroy`: undoing
  turn N+1 also removes the distinct repair row for turn N. This is an
  undo/audit lifecycle issue outside this queue CRUD change.
- Frontend verification: `vue-tsc -b` passed; API focused Vitest 2 passed;
  i18n checks passed. A prior Vite production-assets build completed, but its
  PWA follow-up hit a Windows `EPERM` while resolving the host temp directory.
- Files changed for this step: pending-follow-up domain/contract/repositories,
  admin service/routes/container, admin panel/API/locales/tests, and the
  pending snapshot plus legacy ChatService compatibility fixes.
- Deployment: built local image from `6340238`; verified custom-format backup
  `pre-pending-follow-up-admin-crud-20260830-033122.dump`; `migrate` exited 0;
  only `app` was recreated. PostgreSQL, storage, and WhatsApp sidecar stayed
  running. Health is `ok`, Alembic is `u2c6m8p10046 (head)`, and runtime
  version reports `local-customizations` / `6340238`.
- Next action: monitor the deployed admin queue controls; do not rerun this
  deployment without new source changes.
- Blocked reason: none; do not fold the separate undo/audit failure into this
  narrow queue CRUD change.

## State

- Goal: implement the approved post-turn commitment reconciliation repair.
- Scope: source-only; no deployment, migration execution, live database, or
  user-data changes.
- Current phase: source implementation complete; deployment and migration execution intentionally pending separate approval.
- Completed: processor receives upcoming schedules and active goals; exact-key schedule edits can move across dates; live story beats, active goals, and queued scheduled promises have fail-soft reconciliation paths; legacy non-key adjustments remain supported; scheduled promises persist their commitment key; backup DTOs round-trip commitment identity fields with NULL compatibility; regression coverage pins cross-day moves, first-meeting uniqueness, immutable/ambiguous records, live-goal/beat key matching, and open-promise updates; goals now use first-class validated target dates instead of review-note markers.
- Confirmed constraints: preserve the 30-minute proactive cooldown; keep 16:30
  and 18:00 as separate activities; only one live genuine-first-meeting
  commitment identity; historical records are immutable.
- Rejected paths: re-reading the full reference and redoing repository
  inventory after every compaction; broad unverified patches.
- Last verified commit: `838cacf fix: fence story commitment reconciliation`.
- Last test: `.venv\\Scripts\\python.exe -m pytest tests/unit/test_goal_service.py tests/unit/test_commitment_reconciliation.py tests/unit/test_character_backup_dtos.py tests/unit/test_character_backup_registry.py` — 33 passed; `git diff --check` passed.
- Files changed in this round: `COMMITMENT_RECONCILIATION_IMPLEMENTATION_REFERENCE.md`, `src/kokoro_link/domain/entities/character_goal.py`, `src/kokoro_link/infrastructure/persistence/models.py`, `src/kokoro_link/infrastructure/persistence/sa_goal_repository.py`, `src/kokoro_link/application/dto/character_backup/state_profile.py`, `src/kokoro_link/application/services/turn_snapshot_codec.py`, `src/kokoro_link/application/services/goal_service.py`, `alembic/versions/u2c6m8p10046_commitment_identity.py`, `tests/unit/test_commitment_reconciliation.py`.
- Completed this step: replaced Story Arc commitment reconciliation's stale whole-aggregate save with a repository-level live-beat conditional update; first-meeting clearing remains limited to live beats. Added a deterministic terminal-status race regression test.
- Last verified test: `.venv\\Scripts\\python.exe -m pytest -q tests/unit/test_commitment_reconciliation.py tests/unit/test_story_arc_retire_concurrency.py tests/unit/test_story_arc_service.py tests/unit/test_story_arc_routes.py` — 58 passed.
- Last verified test: `.venv\\Scripts\\python.exe -m pytest -q tests/unit/test_post_turn_schedule_adjustments.py tests/unit/test_post_turn_message_promises.py tests/unit/test_post_turn_cross_day_commitment.py tests/unit/test_post_turn_processor.py tests/unit/test_schedule_service.py tests/unit/test_schedule_service_future_commitment.py tests/unit/test_chat_service.py tests/unit/test_commitment_reconciliation.py tests/unit/test_story_arc_retire_concurrency.py` — 122 passed.
- Completed: durable reference and `UPDATE_PROGRESS_LOG.md` updated; implementation committed as `a30e6cf`. Focused suites passed 122 and 58 tests respectively; compile and diff checks passed.
- Next action: stop here until the user separately approves backup, migration rehearsal, and deployment.
- Stop condition: stop and report if two consecutive resumptions produce no new
  diff, test result, or design decision.
- Blocked reason: none.

## Resume Protocol

On resumption, read this file first, then run `git status --short`, `git log -1
--oneline`, and inspect the current diff. Continue from `Next action`; do not
repeat completed inventory or patch attempts unless the diff contradicts this
checkpoint.

## Upstream Merge Checkpoint (2026-08-28)

- Current implementation step: upstream v0.7.0 merge compatibility repair verified.
- Last verified test: focused commitment/backup/promise/registry tests — 42 passed;
  `git diff --check` passed; `alembic heads` — one head `u2c6m8p10046`.
- Files changed: `llm_planner.py` import, ChatService promise reconciliation
  compatibility, backup DTO legacy-row tolerance, schedule activity ORM fields.
- Next action: checkpoint this repair, then run deployment preflight and
  isolated migration rehearsal before production restart.
- Blocked reason: Docker CLI is not available in the current shell; use the
  configured Docker Desktop/runtime command when available.

## Frontend Merge Compatibility Checkpoint (2026-08-28)

- Current implementation step: restore relationship wizard edit-mode helpers
  lost during upstream v0.7.0 merge.
- Last verified commit: pending checkpoint commit.
- Working tree at checkpoint: dirty (frontend compatibility changes plus the
  previously documented source/migration files).
- Last test: `frontend\\node_modules\\.bin\\vitest.cmd run tests/initialRelationshipForm.test.ts` — 19 passed; `vue-tsc -b` and `vite build` passed (Node 22.9 emitted an advisory version warning).
- Files changed for implementation: `frontend/src/components/InitialRelationshipWizardModal.vue`, `frontend/src/composables/useInitialRelationshipForm.ts`, `frontend/tests/initialRelationshipForm.test.ts`.
- Next action: create narrow checkpoint commit, then run Docker image build and
  isolated PostgreSQL migration rehearsal before touching the runtime.
- Blocked reason: none.

## Deployment Complete Checkpoint (2026-08-28)

- Current implementation step: upstream v0.7.0 merge plus local commitment
  reconciliation is deployed.
- Last verified commit: `e114614 fix: reconcile commitments across projections`
  (with frontend compatibility checkpoint `6baa72a`).
- Working tree at checkpoint: clean.
- Last test: image build passed; backup rehearsal restore/upgrade/downgrade/
  re-upgrade passed; production `migrate` exited 0; app is healthy at
  `http://127.0.0.1:8012/health` (HTTP 200); Alembic=`u2c6m8p10046`;
  counts remain characters=1, schedule_activities=266, pending_follow_ups=55.
- Files changed for implementation: committed source, migration, frontend,
  tests, and durable reference/checkpoint files.
- Next action: monitor runtime logs and user-facing flows; no further migration
  or deployment action is pending.
- Blocked reason: none.

## Deployment Rehearsal Checkpoint (2026-08-28)

- Current implementation step: deployment preflight and isolated migration
  rehearsal complete.
- Last verified commit: pending source checkpoint commit.
- Working tree at checkpoint: dirty until the remaining backend/migration
  changes are committed.
- Last test: local app image build passed; isolated backup restore at
  `t9q4v7x10045` upgraded to `u2c6m8p10046`, required columns were present,
  downgrade/re-upgrade passed, and counts remained characters=1,
  schedule_activities=266, pending_follow_ups=55.
- Files changed for implementation: previously documented backend/domain,
  repository, migration, and reference files.
- Next action: commit the verified source changes, record live pre-restart
  counts, then recreate only migrate/app with both compose files and verify
  health, revision, and unchanged counts.
- Blocked reason: none.

## Legacy Data Repair Live Checkpoint (2026-08-29)

- Current task: execute and deploy the approved repair for legacy 2026-08-30/31
  commitment projections. The durable mapping remains in
  `LEGACY_COMMITMENT_DATA_REPAIR_DRAFT.md`.
- Approved repair committed in one PostgreSQL `SERIALIZABLE` transaction with
  `tools/legacy_commitment_repair_live.sql`; result was
  `LEGACY_REPAIR_COMMITTED`.
- Post-repair counts: characters=1, daily_schedules=22,
  schedule_activities=267, story_arcs=4, story_arc_beats=31,
  character_goals=20, pending_follow_ups=52. The only count changes were the
  approved deletion of one active goal and three duplicate/expired queued
  promises.
- Rollback and commit rehearsals passed in the isolated PostgreSQL instance;
  the verified pre-live dump is
  `legacy-commitment-repair-pre-live-20260829-015633.dump` with SHA-256
  `B1E68E183AB34879DC85CC7955315179E3F9218C198202B89CDD2AE5FCC13B08`.
- Live invariants passed: exactly one live schedule first meeting and one live
  story first meeting; 17:30 card handover and 18:00 festival remain separate;
  8/31 bucket is empty and the active arc premise no longer contains 8/31;
  approved 14:00/16:30 queued promises remain with refreshed delivery-derived
  fields; historical delivery data and the 30-minute proactive cooldown are
  unchanged.
- Deployment verified with the runtime Compose base plus local overlay:
  `migrate` exited 0, Alembic is `u2c6m8p10046 (head)`, and
  `yuralume-app`, PostgreSQL, Storage, and WhatsApp sidecar are healthy.
  `GET http://127.0.0.1:8012/health` returned `status=ok`.
- Last verified commit: `f7bf8b7 local: record legacy repair transaction`.
- Working tree at checkpoint: clean.
- Next action: no further repair or migration action is pending; monitor the
  running app and user-facing schedule/promise views.
- Blocked reason: none.
