# 舊有承諾資料修復執行文件

## 已批准的執行文件

這是一份針對 2026-08-29 提出的正式環境舊有資料修復文件。操作者已於
2026-08-29 批准下方的對應關係與執行／回滾計畫。在本文件更新之前，沒有任何
資料庫資料列、行程、劇情 beat、目標、承諾、角色或聊天記錄被修改。

執行關卡如下，必須依序完成：

1. 操作者審閱並批准下方的對應關係。 **已完成**
2. 建立一份新的、已驗證的 PostgreSQL custom-format 備份。
3. 將確定的修復內容套用到該備份的隔離還原環境中進行演練。
4. 在正式環境以單一交易執行修復，並寫入稽核記錄及變更前後的數量。
5. 驗證變更後的讀取結果、回滾檢查與應用程式健康狀態。

真正送出主動訊息後的現有 30 分鐘 cooldown 不在本次修復範圍內，必須維持
不變。

## 範圍與身份規則

角色：`8b491064-97af-43b1-9124-c89977406120`

劇情線：`6f727ca8102c493faf8af57344b6a7ae`

前置盤點時，下方候選資料列的 `commitment_key` 都是空值（NULL）。本次只會
對已批准的確切 ID 寫入 key；標題、文字、日期及 `delivery_slot_key` 只作為
審閱證據，絕不作為自動合併身份的規則。

活動日的兩個行程仍然分開：

- 17:30-18:00：與桃桃見面並交付深灰卡。
- 18:00-21:30：與桃桃一起參加香港森境Online夏祭。

以上時間是目前實際儲存的香港本地時間。較早的對話曾提到 16:30，但本次
不建議自動改動時間。`delivery_slot_key` 仍然只是發送時段的去重值，不是
承諾身份。

## 建議的標準身份

以下是本次修復採用的固定 key；它們是單一、非空的確切值，只由代表同一
承諾的資料列共用。它們不是 `delivery_slot_key`，也不會被用來回填歷史資料。

| 候選 key | 意義 | 建議的第一次見面旗標 |
| --- | --- | --- |
| `REVIEW-MEET-20260830` | 17:30 的首次實體見面及深灰卡交付 | 在唯一一個 live 行程投影和唯一一個 live 劇情投影上設為 `true` |
| `REVIEW-FESTIVAL-20260830` | 獨立的 18:00-21:30 夏祭活動 | `false` |

已批准的分配：

- `REVIEW-MEET-20260830`：行程活動
  `cc5a2014-e13b-4d5f-8722-1d739f0d824f`、劇情 beat
  `3364666069e5424fbf0e5d9a5faffb37`，以及內容精確吻合的承諾
  `60a72461-99f6-46b1-846d-0f54b144f4e7`。
- `REVIEW-FESTIVAL-20260830`：行程活動
  `d905f512-ac74-45cf-98eb-b04cc779d5aa`，以及內容精確吻合的承諾
  `ff0cbb60-3a04-4df1-9b93-d2ea8dfd1b48`。
- Beat `902e079dfba24e0a83fa7b33d4a92f6b` 是見面前階段，beat
  `8a27eccf52a34ff9bf201113bdc81a73` 是見面後階段。它們不是額外的第一
  次見面；依批准決定維持未綁定的階段 beat。只有 beat `336...` 會設定
  `is_first_meeting=true`。

已批准 17:30-18:00 交卡活動為唯一真正的第一次見面。18:00-21:30
夏祭是見面後的獨立活動，絕不與交卡活動合併，也不設第一次見面旗標。

## 候選資料盤點與建議動作

### 1. 每日行程與活動

| 原始 ID | 所在資料面 | 狀態 | 目前日期／時間（香港） | 目前內容摘要 | 建議動作 | 建議的新日期／文字／時間 | 候選 key | 第一次見面？ | 安全理由／歧義 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `99abb38c-fd76-440f-b5bb-2bfb9f2ab724` | `daily_schedules` | planned；manually adjusted | 2026-08-30 日期分組 | 活動日行程，共 11 個活動 | 保留資料列及其手動編輯邊界；只按確切 ID 就地編輯選定活動，或移動個別子活動 | 保持在 2026-08-30；修復期間不要重新生成這個已手動調整的日期 | 不適用 | 不適用 | 重新生成可能刪除操作者自行建立的活動。 |
| `702a4dd6-9cf6-4a35-ba44-f48f6e944393` | `daily_schedules` | planned；未手動調整；0 個活動 | 2026-08-31 日期分組 | 先前拆分日期後留下的空白日 | 保留為空白／非活動日期分組，除非審閱後有承諾或 beat 明確指定到這天 | 不自動產生或刪除 | 不適用 | 不適用 | 不應為了讓數量看起來對稱而捏造活動。 |
| `cc5a2014-e13b-4d5f-8722-1d739f0d824f` | `schedule_activities` | live；`memorialized=false` | 2026-08-30 17:30-18:00 | 在場地與桃桃見面並交付深灰卡 | 依批准綁定並保留為獨立的 30 分鐘活動 | 保持 2026-08-30 17:30-18:00；保留第一次見面／交卡文字 | `REVIEW-MEET-20260830` | `true` | 唯一的 canonical live first-meeting schedule projection。 |
| `d905f512-ac74-45cf-98eb-b04cc779d5aa` | `schedule_activities` | live；`memorialized=false` | 2026-08-30 18:00-21:30 | 參加香港森境Online線下夏祭 | 依批准綁定並保留為獨立活動 | 保持 2026-08-30 18:00-21:30；保留夏祭文字 | `REVIEW-FESTIVAL-20260830` | `false` | 它接續 17:30 見面，不能與見面活動合併。 |

目前沒有任何行程活動被 memorialized，因此選定的 live 資料列可以在審閱後
修復。如果前置查詢發現其他歷史或 memorialized 活動，必須排除在寫入範圍
之外。

### 2. 目前啟用的劇情線與 beats

| 原始 ID | 所在資料面 | 狀態 | 目前日期 | 目前內容摘要 | 建議動作 | 建議的新日期／文字 | 候選 key | 第一次見面？ | 安全理由／歧義 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `6f727ca8102c493faf8af57344b6a7ae` | `story_arcs.premise` | active arc | premise 表示活動在 2026-08-31 | 劇情線 premise 仍寫香港線下活動在 8/31 | 只有在子項對應獲批准後，才更新目前 aggregate premise | 將活動日期敘述改為 2026-08-30；保留歷史已實現 beat 的文字 | 不適用 | 不適用 | 可修改目前啟用的 aggregate；已實現 beat 仍不可變更。 |
| `902e079dfba24e0a83fa7b33d4a92f6b` | `story_arc_beats`，seq. 5 | pending | 2026-08-30 | 見面前的情緒準備；寫明活動移到 8/30 | 保留日期；只有為保持一致時才修訂含日期的敘述 | 2026-08-30；不新增第一次見面宣稱 | 預設空白；除非明確綁定至 `REVIEW-MEET-20260830` | `false` | 這是前置階段，不是見面本身。 |
| `8a27eccf52a34ff9bf201113bdc81a73` | `story_arc_beats`，seq. 6 | pending | 2026-08-30 | 見面後的卡片／關係反思 | 只有審閱後的標準見面文字需要時才更新文字 | 2026-08-30；不新增第一次見面宣稱 | 預設空白；除非明確綁定至 `REVIEW-MEET-20260830` | `false` | 這是後置階段，不能變成第二個第一次見面。 |
| `3364666069e5424fbf0e5d9a5faffb37` | `story_arc_beats`，seq. 9 | pending | 2026-08-30 | 明確描述第一次線下見面及深灰卡交付 | 依批准作為標準的劇情投影並設定 key／旗標 | 保持 2026-08-30；保留明確的第一次見面／交卡文字 | `REVIEW-MEET-20260830` | `true` | 唯一的 canonical live first-meeting story projection。 |

已實現、skipped 及終止狀態的 beats 不在寫入範圍內。目前啟用劇情線的
premise 是唯一建議修正的 aggregate 文字。

### 3. 目前啟用的目標

目前目標 API 顯示以下 live 候選。它們在資料庫中的 `commitment_key` 和
`target_date_iso` 都是 NULL。

| 原始 ID | 狀態 | 目前內容／日期 | 建議動作 | 建議的新日期／文字 | 候選 key | 與第一次見面的關係 | 安全理由／歧義 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `d7196950-1d60-4f07-b8a6-eb7bdff073ee` | active | 寫著「在 2026-08-31 見面前」 | 依操作者批准刪除；先確認仍是 active 且沒有不可處理的依賴 | 不適用 | 不適用 | 不再保留為目前啟用目標 | 內容同時混入研究討論，不能安全地改寫成單一見面目標。 |
| `431aa2d0-48c8-4300-9800-b3691662e73b` | active | 已寫明 2026-08-30 夏祭 | 保留內容；只有確認目標就是追蹤該次出席時才綁定夏祭身份 | 不改日期／文字 | 只有確認後才使用 `REVIEW-FESTIVAL-20260830` | `false` | 關於創作者工作及保留共同時間的目標，可能比單一活動更廣。 |
| `42d283a3-97b7-4755-9a24-8c099ba55d85` | active | 準備深灰卡；沒有明確日期 | 保持不變；不推斷日期或 key | 不變 | 預設空白 | `false` | 這是準備工作，不是現場交付。 |
| `ba3a2a8a-d485-4c18-a380-c22d6a08f959` | active | 按桃桃的步調安排線下活動；沒有明確日期 | 保持不變；只作為審閱用的相關目標列出 | 不變 | 預設空白 | `false` | 廣泛的關係意圖不能安全地只靠文字匹配。 |

其他目前啟用的目標（健康、技術支援、一般關係工作及無關的商品）不是
候選，必須保持不變。

### 4. 尚待發送的排程承諾／待後續處理

下方五筆在前置盤點時都是 live 的 `kind=scheduled_promise`、`status=queued`
資料列。執行時只會更新已批准的兩筆用途／身份，或刪除已批准的三筆重複／
過期 rows；不會改寫其他排程資料。

| 原始 ID | 目前排程時間（香港） | 內容摘要 | 建議動作 | 建議的新日期／時間或文字 | 候選 key | 與第一次見面的關係 | 安全理由／歧義 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `60a72461-99f6-46b1-846d-0f54b144f4e7` | 2026-08-30 14:00 | 抵達夏祭、與桃桃見面、交付卡片 | 保留 queued；用途改為出發前與玩家聊交卡的期待；保留活動日期並重新計算發送衍生值 | 2026-08-30 14:00；不是活動發生時間 | `REVIEW-MEET-20260830` | `true`（承諾本身沒有第一次見面旗標） | 14:00 是活動前發送時段，17:30 才是實體交卡時間。 |
| `ff0cbb60-3a04-4df1-9b93-d2ea8dfd1b48` | 2026-08-30 16:30 | 聯絡桃桃確認抵達／報到，然後參加夏祭 | 保留 queued；用途改為確認玩家是否快到達；保留獨立夏祭關係並重新計算發送衍生值 | 2026-08-30 16:30；不是 18:00 活動開始時間 | `REVIEW-FESTIVAL-20260830` | `false` | 16:30 是發送時間，與 17:30 交卡及 18:00 夏祭邊界分開。 |
| `31f898bd-36cd-4d54-8597-11ce5ce7f70f` | 2026-08-30 18:00 | 混合多項義務：見面、牽手、夏祭及交卡 | 依操作者批准直接刪除；先確認是 queued live row 並處理所有可發送依賴 | 不適用 | 不適用 | 不再作為 live 承諾 | 與已保留的兩筆排程重複且無法安全拆成單一身份。 |
| `29ea8f5e-2a21-41c5-9b23-ccc624bf23e2` | 2026-08-31 12:00 | 8/31 見面並討論研究邀請／關係 | 依操作者批准直接刪除；先確認是 queued live row 並處理所有可發送依賴 | 不適用 | 不適用 | 不再作為 live 承諾 | 日期過期且混入獨立研究討論，不能移動成單一見面承諾。 |
| `a0a025a8-8be4-4c39-bcc8-90676795d2d7` | 2026-08-31 14:00 | 多項合併義務，明確描述 8/31 的第一次線下見面 | 依操作者批准直接刪除；先確認是 queued live row 並處理所有可發送依賴 | 不適用 | 不適用 | 不再作為 live 承諾 | 與 8/30 canonical 見面及保留排程重複。 |

任何被移動的承諾，都必須一起重新計算 `scheduled_for`、時區轉換及所有
發送衍生的去重欄位。絕不能把 `delivery_slot_key` 複製到
`commitment_key`。

## 建議的最小寫入範圍

依已批准的標準對應，採用以下最小修復：

1. 將目前啟用劇情線的 premise 從 8/31 修正為 8/30，同時保留所有已實現／
   終止 beat 的文字。
2. 將 `cc5...` 和 beat `336...` 綁定至批准的見面 key，並只在這兩個 live
   投影上設定 `is_first_meeting=true`。
3. 將 `d905...` 綁定至批准的夏祭 key，並設定 `is_first_meeting=false`。
4. 刪除 active goal `d719...`；刪除前檢查並處理其明確依賴，不改其他目標。
5. 保留 `60a...`／`ff0...` 的 queued 排程與指定發送用途；刪除 `31f...`、
   `29ea...`、`a0...` 三筆 queued 重複／過期資料及其可發送依賴。硬刪除只限
   這三個明確 ID，不擴大到歷史或其他狀態。
6. 除非之後的審閱明確擴大寫入範圍，否則保持前置／後置 beats、準備卡片
   目標、廣泛的線下安排目標、空白的 8/31 行程、歷史資料、對話及 cooldown
   狀態不變。

## 已批准的決定

- 真正的第一次見面是 17:30-18:00 交卡；18:00-21:30 是見面後獨立的夏祭活動。
- Beat `902...` 與 `8a27...` 維持未綁定的階段 beats；只有 beat `336...` 是
  canonical first-meeting story projection。
- 目標 `d719...` 整筆刪除，不綁定任何 key。
- `31f...`、`29ea...`、`a0...` 三筆 queued promise 直接刪除；只先處理它們
  各自的可發送依賴，不刪除其他資料。
- 保留 14:00 `60a...`（出發前聊交卡期待）與 16:30 `ff0...`（確認玩家是否
  快到達）；兩者是發送時段，不是活動邊界。
- 8/30 手動調整行程保留；8/31 空白 bucket 保留；真正送出主動訊息後的
  30 分鐘 cooldown 保持不變。

## 執行與回滾計畫（只在批准後進行）

1. 重新執行唯讀前置查詢，逐一核對每個候選 ID、狀態、目前 key 及日期與
   本初稿一致。
2. 在 runtime 的 `backups` 目錄建立新的 PostgreSQL custom-format dump，並
   以 `pg_restore -l` 驗證。
3. 將 dump 還原到隔離的演練資料庫。在該環境套用確定的交易，驗證 key／旗標／
   日期／文字不變式；接著回滾並再次檢查，證明原始數量與內容可恢復。
4. 在正式環境以單一交易執行，並寫入範圍狹窄的稽核記錄：原始 ID、舊值、新
   值、操作者批准的決定及 UTC 時間戳。不記錄密鑰或完整聊天 payload。
5. 驗證變更後的資料列數量、唯一一個 live 第一次見面、沒有歷史資料列被
   修改、17:30／18:00 活動仍分開、承諾的發送欄位已重新計算，以及主動
   cooldown 維持不變。
6. 若驗證失敗，回滾該交易。已驗證的 dump 只作為復原途徑；絕不要藉由刪除
   大範圍資料表或重新生成已手動調整的 8/30 行程來補救。

## 目前狀態

- 修復文件已完成繁中化並記錄操作者批准的 mapping。
- 唯讀 preflight 已完成：正式環境 health=`status=ok`，Alembic revision
  `u2c6m8p10046`；數量為 characters=1、daily_schedules=22、
  schedule_activities=267、story_arcs=4、story_arc_beats=31、
  character_goals=21、pending_follow_ups=55。
- 已建立並驗證備份：
  `C:\\Entertainment\\yuralume\\backups\\legacy-commitment-repair-pre-20260829-012533.dump`；
  custom archive 640 個 TOC 項目、107243296 bytes；SHA-256 為
  `DEB10A1FAA83703C30832D2EB3C97002D04CFE07638A25363B47100D830DFAC`。
- 正式環境資料變更：無。
- 隔離 rehearsal／正式交易／部署：尚未開始。
- 下一步：將已驗證備份還原至明確隔離的 PostgreSQL，先演練 rollback，
  再演練 commit 與所有不變式。
