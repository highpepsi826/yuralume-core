# 待修：避免上下文精簡造成工作循環

## 目的

處理近期上下文精簡頻繁時，代理反覆重新閱讀 reference、重新盤點檔案，
卻沒有繼續實作的問題。這份文件是執行 checkpoint，不取代產品設計
reference。

## 根因

- 上下文精簡後，聊天歷史不是可靠的進度來源。
- `COMMITMENT_RECONCILIATION_IMPLEMENTATION_REFERENCE.md` 目前主要記錄
  設計規格，缺少可直接恢復的目前步驟。
- `UPDATE_PROGRESS_LOG.md` 是已驗證工作的歷史紀錄，不適合當即時 checkpoint。
- 大範圍 patch 失敗後沒有留下小步驟提交，恢復時容易重新開始。

## 執行協議

1. 每次開始先只讀本文件的「目前狀態」、`git status --short`、最近一次
   commit，以及目前 diff；不要重新完整盤點已完成的檔案。
2. 每回合只處理一個小步驟，完成後立即執行 `git diff --check` 和相關的
   focused tests。
3. 測試通過後，先更新本文件的「目前狀態」，再建立窄範圍 checkpoint commit。
4. 測試失敗時記錄失敗原因、已排除的假設和下一個替代動作，避免原樣重試。
5. 若上下文再次精簡，直接從 `Next action` 繼續；不得回到完整 reference
   閱讀或全量盤點，除非目前 diff 顯示規格已改變。
6. `COMMITMENT_RECONCILIATION_IMPLEMENTATION_REFERENCE.md` 仍是唯一的
   行為規格；任何設計變更先寫入該文件，再修改程式碼。

## 本專案採用的 Workaround

這些措施不能修復 Codex 的 compaction 引擎，但適合目前這項跨 domain、
persistence、parser 和 service 的 Yuralume 修改：

- 持久 checkpoint：保留本文件，不使用聊天歷史作為唯一進度來源。只記錄
  工作狀態、決策、測試結果與下一個動作，不記錄 API key、連線字串、聊天
  內容或使用者資料。
- 小步驟與窄範圍提交：按 reference 的 implementation order 分段，每段只
  改一組相鄰檔案；通過 focused tests 後才進下一段。這可讓 `git diff` 和
  最近 commit 成為可驗證的第二份狀態來源。
- 恢復前置檢查：上下文精簡或新回合開始時，只讀本文件目前狀態、
  `git status --short`、最近 commit 和目前 diff；除非規格或 diff 改變，
  不重新完整閱讀 reference 或重做已完成的盤點。
- 階段邊界：domain/persistence、post-turn contract、schedule、story arc、
  goals/promises、ChatService、tests 各自形成可驗證階段。必要時在階段
  完成後結束回合，再以 checkpoint 繼續，減少在大型 patch 中途觸發 compaction。
- Loop 停止條件：若連續兩次恢復都沒有新的 diff、測試結果或設計決策，
  立即停止重試，記錄 `Blocked reason` 並向使用者回報；不得以相同順序重讀
  檔案或重送相同 patch。這是停止失控消耗的保護，不代表自動判定工作失敗。

### 不採用的部分

- 不把自由格式的百分比進度當作真實狀態；以步驟、commit、diff 和測試結果
  為準。
- 不另建會與本文件分叉的第二份產品規格；行為規格仍只在
  `COMMITMENT_RECONCILIATION_IMPLEMENTATION_REFERENCE.md`。
- 不因 workaround 而部署、執行 migration 或修改 live data；這些仍受本次
  source-only 範圍和 `AGENTS.md` 約束。

## 目前狀態

- Repository: `C:\Entertainment\yuralume-src`
- Branch: `local/customizations`
- Last verified commit: `f79001f local: record commitment reconciliation plan`
- Working tree at checkpoint: dirty (checkpoint files pending commit)
- Current implementation step: post-turn contracts/parser/prompt and ChatService exact-key reconciliation
- Last test: `.venv\\Scripts\\python.exe -m pytest tests/unit/test_post_turn_processor.py tests/unit/test_post_turn_schedule_adjustments.py tests/unit/test_post_turn_message_promises.py tests/unit/test_schedule_adjustments.py` — 51 passed; compile and `git diff --check` passed
- Files changed for implementation: post-turn contracts/parser/prompt, ChatService, schedule service, story arc service, plus prior domain/persistence/migration files
- Next action: add focused reconciliation regression tests, then inspect and checkpoint commit
- Blocked reason: none

## 目前工作的固定邊界

實作目標與不可變資料規則以
`COMMITMENT_RECONCILIATION_IMPLEMENTATION_REFERENCE.md` 為準。尤其保留
真正送出主動訊息後的 30 分鐘 cooldown、允許 16:30 與 18:00 為不同活動，
並只讓同一個 live commitment identity 成為 genuine first meeting。

本輪 source-only：不部署、不執行 migration、不改 live database 或使用者資料。

## 網路查證（2026-08-28）

Tavily MCP 在本次工作階段不可用：沒有提供 Tavily 搜尋工具，MCP resource
與 template 也都是空集合。因此以下使用瀏覽器作替代搜尋；這些結果不是
Tavily 結果。

已找到與本問題高度相符的公開回報：

- OpenAI Codex issue [#25792](https://github.com/openai/codex/issues/25792)
  描述 compaction 後遺失 AGENTS 規則與進度，工作可能由接近完成退回較早階段。
- OpenAI Codex issue [#25900](https://github.com/openai/codex/issues/25900)
  描述 compaction 成功但遺失 continuation checkpoint，導致重讀檔案、重複分析，
  並明確建議保存 `CURRENT_TASK`、`CURRENT_PHASE`、`NEXT_ACTION`、
  `STOP_CONDITION` 等結構化狀態；該 issue 已標為 duplicate。
- OpenAI Codex issue [#35226](https://github.com/openai/codex/issues/35226)
  描述 2026 年 7 月 Windows/macOS 長任務在 compaction 後反覆讀相同檔案、
  反覆重述計畫而不修改工作樹，並消耗使用額度。
- OpenAI Codex issue [#35935](https://github.com/openai/codex/issues/35935)
  描述 2026 年 7 月 Windows 上的相同 regression：完成 build/驗證附近後，
  compaction 讓代理回到早期階段並重做工作。

這表示你的觀察不是孤立案例，也符合近期公開 issue 的共同症狀。這些 issue
目前沒有提供使用者端可以立即開啟的根治設定；部分報告已標為 duplicate，
所以不能說 Codex 已經修好。另有社群回覆指出，使用持久的 ExecPlan 或小型
round-state file 可以降低風險，但這是 workaround，不是產品層修復。

可採用的實務解法（按可靠度排序）：

1. 將目前 task 的 `GOAL`、`SCOPE`、`FACTS`、`REJECTED`、`NEXT`、`STOP`
   寫入 repository 內的持久 Markdown checkpoint；本文件就是此用途的專案級
   版本。
2. 每個小步驟後跑 focused test、更新 `Next action`，並建立窄範圍 git commit，
   讓工作樹和 checkpoint 可以互相驗證。
3. 上下文恢復時只讀 checkpoint、`git status`、最近 commit 和目前 diff；若沒有
   工作樹進展，不重做同一輪檔案盤點。
4. 長任務拆成多個可驗證階段，必要時在階段邊界主動結束一回合，再以同一份
   checkpoint 繼續，降低自動 compaction 在程式修改中途觸發的機會。
5. 若連續兩次恢復都沒有新的 diff、測試或決策，停止並報告 loop，而不是繼續
   消耗額度；先檢查 checkpoint 和實際 repository 狀態。

OpenAI Developers 的搜尋結果也指向 long-running work、Goals 與 memory/
compaction 相關文件，方向上支持「明確 outcome、definition of done、可持續
的 task state」；但該官方頁面在本次瀏覽環境回傳 HTTP 403，未把搜尋摘要當成
完整官方行為承諾。

## Checkpoint 格式

每次完成小步驟後更新以下欄位：

```text
Current implementation step: <number>
Last verified commit: <commit>
Working tree at checkpoint: clean|dirty
Last test: <command and result>
Files changed for implementation: <paths>
Next action: <one concrete action>
Blocked reason: <none or specific reason>
```
